# Gap Analysis — groups-mobile-2col (모바일 구역관리 2열 + 편집모드 + 드래그 스크롤 수정)

- **작성일**: 2026-07-15
- **범위 커밋**: `79b44be`, `0bb681b`, `5f0d26e`, `b559ba8`, `e9c21a2`
- **핵심 질문**: UI 변경과 무관한 **기능상 영향**이 있는가? → **없음 (검증 완료)**
- **Match Rate**: **96%**

---

## 1. 변경 요약

| 커밋 | 내용 | 성격 |
|------|------|------|
| 79b44be | 모바일 카드 밀도(여백만) | CSS |
| 0bb681b | 모바일 2열 배치 + `useReorder` 열 인식(2열=X축, 1열=Y축) | CSS + 로직 |
| 5f0d26e | 라벨 축약, 라벨 nowrap, 모바일 ✎ 숨김, **전역 `.main-content` 여백 축소** | CSS(전역) |
| b559ba8 | 구역별 **편집 모드**(편집 시에만 손잡이·이름수정 노출), 이름 인라인 수정, 자동 포커스 | 로직(Groups 한정) |
| e9c21a2 | `useReorder` **터치 드래그 후 스크롤 사망 수정**(암묵적 포인터 캡처 해제 + 정리 보장) | 로직(공용) |

---

## 2. 기능 영향 매트릭스 (UI 외 파급)

| 잠재 영향 지점 | 판정 | 근거 |
|---|---|---|
| **토글 제어 로직**(원격제어/액비·교반기/개폐기/유동팬) | ✅ 영향 없음 | 라벨 텍스트만 축약. 제어 키 `remote_control`/`fertilizer_b_contact` 및 핸들러 불변 |
| **정식 라벨 매핑**(device.types, GatewayEnvSettings, 자동화 조건화면) | ✅ 영향 없음 | Groups 카드의 표시 문자열만 변경. 매핑 상수는 독립 유지 |
| **공용 `useReorder` 소비자 — Automation(룰 순서)** | ✅ 정상 | 부모 `.zone-card`=block → `gridTemplateColumns:none` → cols=1 → **Y축(기존과 동일)**. 런타임 재정렬 `PATCH 200`, 스크롤 누수 없음 |
| **공용 `useReorder` 소비자 — ZoneVisibilityModal(구역 순서)** | ✅ 정상(정적) | 부모 `.house-list`(ul)=block → cols=1 → Y축 동일. 스크롤 수정은 순수 정리-보장 강화 |
| **열 인식 분기**(`getComputedStyle` 열 수) | ✅ 안전 | grid가 아닌 컨테이너는 'none'→1열로 처리 → 오직 Groups 장치 grid(2열)만 X축 사용 |
| **전역 여백**(`.main-content`, App.vue) | ⚠️ 시각적 전역 | `main-content`는 App.vue에만 존재. 로직 없음. 데스크탑/모바일 여러 화면 스샷 확인(대시보드·구역·자동화 정상) |
| **이름 인라인 수정 상태**(renamingDeviceId/OpenerGroup) | ✅ 안전 | `cancelDeviceRename`이 두 상태 모두 리셋 → 자동 포커스 대상 항상 단일 |
| **권한**(farm_user) | ✅ 불변 | 편집 버튼·손잡이·이름수정 모두 `!isFarmUser` 유지 |
| **백엔드/데이터/API** | ✅ 무변경 | 프론트 CSS·이벤트 처리만 수정. reorder API는 기존 그대로 |

---

## 3. 런타임 검증 (Playwright — 실제 마우스/터치 이벤트)

| 시나리오 | 결과 |
|---|---|
| Groups 같은 그룹 재정렬(터치) | 순서변경 ✅ `PATCH 200` |
| Groups 드래그 직후 스크롤 | 동작 ✅ (scrollY 0→779) |
| Groups 재정렬 후 리스너 누수 | 없음 ✅ |
| Automation 룰 재정렬(마우스) | 순서변경 ✅ `PATCH 200`, 누수 없음 |
| 편집 OFF 상태 | 손잡이·✎ 미노출 ✅ |
| 편집 ON → 이름 클릭 | 입력창 전환 + 자동 포커스 ✅ |

---

## 4. Gap / 잔여 항목

| # | 항목 | 상태 | 비고 |
|---|------|------|------|
| G1 | 장치 하위 3그룹(개폐기/관주/일반) 간 드래그 이동 불가 | **설계 의도** | 각 그룹 독립 정렬(사양) — 갭 아님 |
| G2 | ZoneVisibilityModal 재정렬 런타임 미실행(정적만) | 경미 | 동일 코드경로 + 부모 block 확인으로 커버. 필요 시 추가 E2E |
| G3 | `Groups.vue.backup` 잔존(구 라벨 포함) | 무관 | 미사용 백업 파일(빌드 제외). 정리 권장 |

---

## 5. 결론

- **UI 변경과 무관한 기능상 부작용은 발견되지 않음.**
- 공용 컴포저블(`useReorder`) 변경은 다른 두 화면에서 **동작 불변**(1열=Y축) + **스크롤 정리 강화**만 적용됨을 정적·런타임으로 확인.
- 라벨 축약·전역 여백은 순수 표시 계층. 제어 로직·데이터·권한·API 무변경.
- **Match Rate 96%** (잔여는 설계 의도 및 경미 항목).
